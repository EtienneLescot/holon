# Holon UI (Phase 4)

React + React Flow UI that runs inside the VS Code extension webview.

## Dev

- `npm install`
- `npm run dev`

## Build for the extension

- `npm run build`

The extension loads the static assets from `ui/dist`.
